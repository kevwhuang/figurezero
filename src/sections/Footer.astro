---
import { ROUTES } from '@lib/constants';

const EMAIL = 'figure.zero.project@gmail.com';
const INSTAGRAM = 'figure.zero.project';
---

<footer class="pb-8 pt-(--pad-block) border-t border-charcoal">
    <div class="shell">
        <div class="grid grid-cols-12 items-start gap-7 mb-(--gap-block) max-md:*:last:mb-0 max-md:*:mb-10 max-md:block">
            <div class="col-span-3 col-start-1">
                <span class="footer__logo block aspect-square w-(--logo-size) bg-current mask-center mask-contain mask-no-repeat translate-x-[calc(var(--logo-size)_*_-0.13)] translate-y-[calc(var(--logo-size)_*_-0.08)]" aria-label="Figure Zero Project" role="img" />
            </div>
            <div class="col-span-4 col-start-5 max-lg:col-start-4">
                <p
                    id="footer-navigation-title"
                    class="eyebrow mb-5"
                >
                    Navigation
                </p>
                <nav aria-labelledby="footer-navigation-title">
                    <ul class="grid gap-2.5">
                        {ROUTES.map(route => (
                            <li>
                                <a
                                    class="active:translate-y-px after:-inset-y-[5px] after:absolute after:inset-x-0 hover:text-taupe inline-block relative no-underline text-[15px] wrap-anywhere text-charcoal duration-(--duration-fast) ease-[ease] transition-[color,translate]"
                                    href={route.href}
                                >
                                    {route.label}
                                </a>
                            </li>
                        ))}
                    </ul>
                </nav>
            </div>
            <div class="col-span-4 col-start-9 max-lg:col-span-5 max-lg:col-start-8">
                <p
                    id="footer-contact-title"
                    class="eyebrow mb-5"
                >
                    Contact
                </p>
                <address
                    class="not-italic"
                    aria-labelledby="footer-contact-title"
                >
                    <ul class="grid gap-2.5">
                        <li>
                            <a
                                class="active:translate-y-px after:-inset-y-[5px] after:absolute after:inset-x-0 hover:text-taupe inline-block relative no-underline text-[15px] wrap-anywhere text-charcoal duration-(--duration-fast) ease-[ease] transition-[color,translate]"
                                href={`mailto:${EMAIL}`}
                            >
                                {EMAIL}
                            </a>
                        </li>
                        <li>
                            <a
                                class="active:translate-y-px after:-inset-y-[5px] after:absolute after:inset-x-0 hover:text-taupe inline-block relative no-underline text-[15px] wrap-anywhere text-charcoal duration-(--duration-fast) ease-[ease] transition-[color,translate]"
                                href={`https://instagram.com/${INSTAGRAM}`}
                                rel="noopener"
                                target="_blank"
                            >
                                @{INSTAGRAM}
                            </a>
                        </li>
                    </ul>
                </address>
            </div>
        </div>
        <div class="pt-6 border-t border-linen">
            <p class="eyebrow footer__copyright">&copy; 2026 Figure Zero Project</p>
        </div>
    </div>
</footer>

<style>
    .footer__copyright {
        letter-spacing: 0.16em;
    }

    .footer__logo {
        --logo-size: clamp(128px, calc(117.33px + 3.33vw), 160px);
        mask-image: url('../images/logos/figure_zero.webp');
    }
</style>
