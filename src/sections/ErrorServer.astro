<section
    class="section grid place-items-center min-h-[60svh]"
    aria-labelledby="error-server-title"
>
    <div
        class="shell text-center"
        data-scroll
    >
        <h1
            id="error-server-title"
            class="error-server__title"
        >
            500
        </h1>
        <p class="error-server__text">Something went wrong. Please try again.</p>
        <a
            class="error-server__link"
            href="/"
        >
            Back to home
        </a>
    </div>
</section>

<style>
    .error-server__link {
        border-bottom: 1px solid var(--color-charcoal);
        display: inline-block;
        font-family: var(--font-sans);
        font-size: 11px;
        letter-spacing: 0.16em;
        padding-bottom: 2px;
        position: relative;
        text-transform: uppercase;
        transition:
            border-color var(--duration-fast) ease,
            color var(--duration-fast) ease,
            transform var(--duration-fast) ease;
    }

    .error-server__link:active {
        transform: translateY(1px);
    }

    .error-server__link:hover {
        border-color: var(--color-taupe);
        color: var(--color-taupe);
    }

    .error-server__link::after {
        content: '';
        inset: -14px;
        position: absolute;
    }

    .error-server__text {
        color: var(--color-umber);
        font-size: 17px;
        margin-bottom: 40px;
    }

    .error-server__title {
        font-size: clamp(96px, calc(53.33px + 13.33vw), 224px);
        font-weight: 480;
        line-height: 0.9;
        margin-bottom: 24px;
    }
</style>
